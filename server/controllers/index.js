import Personnel from './Personnel.controller.js'

// try/catch block or express-async-handler like:
// const test = asyncHandler(async(req, res) =? {
//     res.status(200).json({test: 'test'})
// }

export default {
    Personnel,
};